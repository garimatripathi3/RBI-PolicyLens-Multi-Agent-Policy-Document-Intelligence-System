services:
  policylens:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Mount data so you can drop new policy files without rebuilding.
      - ./data:/app/data
    restart: unless-stopped
